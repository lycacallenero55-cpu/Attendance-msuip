# GPU Training Requirements
# Optimized for AWS GPU instances with CUDA support

# Core ML/DL with GPU support
tensorflow[and-cuda]==2.18.0
# Alternative: tensorflow-gpu==2.13.0  # Use if tensorflow[and-cuda] not available

# Core dependencies
numpy==1.24.3
scikit-learn==1.3.2
scipy==1.10.1
pillow==10.2.0
opencv-python==4.9.0.80

# AWS/S3
boto3==1.34.34
botocore==1.34.34

# Web framework
fastapi==0.92.0
starlette==0.25.0
uvicorn[standard]==0.22.0
python-multipart==0.0.9

# Database and HTTP
supabase>=2.6.0
httpx>=0.27,<0.28
requests==2.31.0
python-dotenv==1.0.1

# Serialization
orjson==3.9.15

# Pydantic v1 to match FastAPI 0.92
pydantic==1.10.13

# Async HTTP client
aiohttp>=3.8,<4

# Core typing pin to satisfy TensorFlow
typing-extensions==4.5.0

# Additional GPU optimization libraries
# cuDNN is included with TensorFlow GPU
# CUDA toolkit should be pre-installed on GPU instances

# Optional: For better performance
# nvidia-ml-py3  # For GPU monitoring
# psutil  # For system monitoring